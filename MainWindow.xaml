<Window x:Class="PhotoFolderViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:PhotoFolderViewer.Converters"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Width="1680"
        Height="980"
        MinWidth="1200"
        MinHeight="720"
        WindowStartupLocation="CenterScreen"
        Background="#0F1318"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBg" Color="#0F1318" />
        <SolidColorBrush x:Key="PaneBg" Color="#151B22" />
        <SolidColorBrush x:Key="PaneBorder" Color="#26303B" />
        <SolidColorBrush x:Key="TextPrimary" Color="#E3EBF4" />
        <SolidColorBrush x:Key="TextMuted" Color="#92A1B4" />
        <SolidColorBrush x:Key="Accent" Color="#55C6FF" />
        <SolidColorBrush x:Key="ThumbSelected" Color="#203245" />
        <SolidColorBrush x:Key="ThumbHover" Color="#1A2733" />
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Margin" Value="6,4" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource ThumbHover}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource ThumbSelected}" />
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource Accent}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="Background" Value="#1A232C" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="#18222D" />
            <Setter Property="BorderBrush" Value="#2E3C4C" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBg}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding ThumbnailPaneWidth}" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{StaticResource PaneBg}"
                BorderBrush="{StaticResource PaneBorder}"
                BorderThickness="0,0,1,0">
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Top"
                           Margin="14,14,14,10"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextMuted}"
                           Text="Folder Photos" />

                <ListBox x:Name="ThumbnailList"
                         Margin="8,0,8,8"
                         Background="Transparent"
                         BorderThickness="0"
                         ItemsSource="{Binding Images}"
                         SelectedItem="{Binding SelectedImage, Mode=TwoWay}"
                         ScrollViewer.CanContentScroll="True"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         VirtualizingPanel.ScrollUnit="Pixel">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Border Width="225"
                                        Height="126"
                                        Background="#11171D"
                                        BorderBrush="#2E3B49"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        SnapsToDevicePixels="True">
                                    <Grid>
                                        <Image Stretch="UniformToFill"
                                               Source="{Binding Thumbnail}" />
                                        <TextBlock HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Foreground="{StaticResource TextMuted}"
                                                   FontSize="11"
                                                   Text="Loading..."
                                                   Visibility="{Binding Thumbnail, Converter={StaticResource NullToVisibilityConverter}}" />
                                    </Grid>
                                </Border>
                                <TextBlock Margin="4,6,4,0"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{StaticResource TextMuted}"
                                           FontSize="11"
                                           Text="{Binding FileName}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="0">
            <Border Margin="18"
                    Padding="12"
                    Background="#10161D"
                    BorderBrush="{StaticResource PaneBorder}"
                    BorderThickness="1"
                    CornerRadius="10">
                <ScrollViewer x:Name="MainImageScrollViewer"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Auto"
                              PreviewMouseWheel="MainImageScrollViewer_PreviewMouseWheel">
                    <Grid>
                        <Image x:Name="MainImage"
                               Source="{Binding CurrentImage}"
                               Stretch="{Binding ViewerStretch}"
                               RenderOptions.BitmapScalingMode="HighQuality">
                            <Image.RenderTransform>
                                <ScaleTransform ScaleX="{Binding ZoomFactor}"
                                                ScaleY="{Binding ZoomFactor}" />
                            </Image.RenderTransform>
                            <Image.ContextMenu>
                                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                    <MenuItem Header="Zoom In" Command="{Binding ZoomInCommand}" />
                                    <MenuItem Header="Zoom Out" Command="{Binding ZoomOutCommand}" />
                                    <Separator />
                                    <MenuItem Header="Actual Size (100%)" Command="{Binding ActualSizeCommand}" />
                                    <MenuItem Header="Fit to Screen" Command="{Binding FitToScreenCommand}" />
                                    <Separator />
                                    <MenuItem Header="Delete Photo" Click="DeletePhotoMenuItem_Click" />
                                    <Separator />
                                    <MenuItem Header="{Binding SlideshowMenuText}" Command="{Binding ToggleSlideshowCommand}" />
                                    <MenuItem Header="Full Screen Mode" Command="{Binding ToggleFullScreenCommand}" />
                                </ContextMenu>
                            </Image.ContextMenu>
                        </Image>

                        <TextBlock HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="16"
                                   Foreground="{StaticResource TextMuted}"
                                   Text="No image selected"
                                   Visibility="{Binding CurrentImage, Converter={StaticResource NullToVisibilityConverter}}" />
                    </Grid>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>
